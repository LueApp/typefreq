[Unit]
Description=typefreq — personal typing analytics
After=graphical-session.target
PartOf=graphical-session.target

[Service]
Type=simple
# `sg input` lets the service read /dev/input/event* even when the
# user-manager's group list was snapshotted before the user joined `input`.
ExecStart=/usr/bin/sg input -c "__INSTALL_DIR__/venv/bin/python -m typefreq.app"
WorkingDirectory=__INSTALL_DIR__
Restart=on-failure
RestartSec=5
Environment=TYPEFREQ_HOST=127.0.0.1
Environment=TYPEFREQ_PORT=__TYPEFREQ_PORT__
Environment=TYPEFREQ_PUBLIC_SITE=__TYPEFREQ_PUBLIC_SITE__
Environment=TYPEFREQ_ALLOWED_ORIGINS=__TYPEFREQ_ALLOWED_ORIGINS__
# Notifications need access to the user's bus.
Environment=DBUS_SESSION_BUS_ADDRESS=unix:path=%t/bus
# Pass through display vars for notify-send.
PassEnvironment=DISPLAY WAYLAND_DISPLAY XDG_RUNTIME_DIR

[Install]
WantedBy=graphical-session.target
